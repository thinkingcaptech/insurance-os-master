{
  "name": "insurance-os",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --input=src",
    "build": "npx @11ty/eleventy --input=src"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
